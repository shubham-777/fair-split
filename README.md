alembic revision --autogenerate -m "initial setup" --rev-id=001

Pending:
    Expense and ExpenseSplit Feature
    Split into separate view files
    Correct ExpensiveSplit endpoint urls too long right now
    
======================================================
